<?xml version='1.0' encoding='utf-8'?>
<widget id="com.smartbar.smartprint"
        version="1.0.0"
        xmlns="http://www.w3.org/ns/widgets"
        xmlns:cdv="http://cordova.apache.org/ns/1.0">
  <name>SmartPrint</name>
  <description>Impresión automática para POS</description>
  <author email="you@example.com">You</author>

  <!-- Preferencias -->
  <preference name="AndroidPersistentFileLocation" value="Compatibility" />
  <preference name="Autostart" value="true" />
  <preference name="BackgroundMode" value="enabled" />

  <!-- Plugins -->
  <plugin name="cordova-plugin-background-mode" />
  <plugin name="cordova-plugin-autostart" />
  <plugin name="cordova-plugin-network-information" />
  <plugin name="cordova-plugin-chrome-apps-sockets-tcp" />
  <plugin name="cordova-plugin-printer" />

  <!-- Permisos Android -->
  <edit-config file="AndroidManifest.xml" mode="merge" target="/manifest">
    <manifest>
      <uses-permission android:name="android.permission.INTERNET" />
      <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
      <uses-permission android:name="android.permission.WAKE_LOCK" />
      <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
      <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
      <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
      <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
      <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    </manifest>
  </edit-config>

  <!-- Plataformas -->
  <platform name="android">
    <!-- (opcional) iconos/splash -->
  </platform>

  <content src="index.html" />
  <access origin="*" />
  <allow-intent href="*" />
</widget>
